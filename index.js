/**
 * @format
 */

import {AppRegistry} from 'react-native';
import PerfilPalestrante from './PerfilPalestrante';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => PerfilPalestrante);
